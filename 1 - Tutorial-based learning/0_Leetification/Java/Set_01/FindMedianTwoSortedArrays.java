package Set_01;

public class FindMedianTwoSortedArrays {

  // INCOMPLETE

  // public double findMedianSortedArrays(int[] nums1, int[] nums2) {
  // int n = nums1.length;
  // int m = nums2.length;

  /* If the length of the first array is greater than the second: */
  // if (n > m) {
  /*
   * Recursive call on the function, switching their places. Reasoning: We want n
   * containing the bigger array of the two.
   */
  // return findMedianSortedArrays(nums2, nums1);
  // }
  // int i = 0;
  // int j = 0;
  // int imin = 0;
  // int imax = n;
  // int half = (n+m+1)/2;
  // int maxLeft = 0;
  // int minRight = 0;

  // while (imin <= imax) {
  // i = (imin + imax)/2;
  // j = half - i;
  // if (i > 0 && j < m && nums1[i-1] > nums2[j]) {
  // imax = i - 1;
  // }
  // }
  // }
}
